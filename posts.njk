---
title: Posts
description: "Posts by Aaron Pinero."
keywords: "design, graphic design, web development, front-end development, web design"
theme: "green-pink"
layout: page.njk
eleventyNavigation:
  key: Posts
  order: 2
pagination:
  data: collections.allPosts
  size: 20
  alias: allposts
---
  <div class="status">Posts {{ (pagination.pageNumber * pagination.size) + 1 }} — {{ (pagination.pageNumber * pagination.size) + pagination.items.length }} of {{ collections.allPosts.length }}</div>
  <nav class="pagination" aria-labelledby="posts-pagination-top">
    <h2 id="posts-pagination-top" class="visually-hidden">Posts Pages</h2>
    <ul>
      <li>
        {% if page.url != pagination.href.first %}
          <a href="{{ pagination.href.previous }}" title="Previous" class="previous"><span class="picons-regular-angle"></span> <span class="visually-hidden">Previous</span></a>
        {% else %}
          <span class="previous"><span class="picons-regular-angle"></span> <span class="visually-hidden">Previous</span></span>
        {% endif %}
      </li>
    
      {% for pageEntry in pagination.pages %}
        <li>
          <a
            href="{{ pagination.hrefs[ loop.index0 ] }}"
            {% if page.url == pagination.hrefs[ loop.index0 ] %} aria-current="page"{% endif %}
          >
            <span class="visually-hidden">Page </span>{{ loop.index }}
          </a>
        </li>
      {% endfor %}
      
      <li>
        {% if page.url != pagination.href.last %}
          <a href="{{ pagination.href.next }}" title="Next" class="next"><span class="picons-regular-angle"></span> <span class="visually-hidden">Next</span></a>
        {% else %}
          <span class="next"><span class="picons-regular-angle"></span> <span class="visually-hidden">Next</span></span>
        {% endif %}
      </li>
    </ul>
  </nav>

<div class="allposts">
  {% for post in allposts %}
    {% if post.data.title %}
      <article class="post article">
        <div class="post-meta">What I Learned On <time datetime="{{ post.date | dateIso }}">{{ post.date | dateReadable }}</time></div>
        <h2 class="post-title"><a href="{{ post.url | url }}">{{ post.data.title }}</a></h2>
        <p>{{ post.data.excerpt }}</p>
      </article>
    {% else %}
      <article class="post micro">
        <div class="post-meta"><time datetime="{{ post.date | dateIso }}">{{ post.date | dateReadable }}</time> – {{ post.data.time }}</div>
        {{ post.content | safe }}
      </article>
    {% endif %}
  {% endfor %}
</div>

  <nav class="pagination" aria-labelledby="archive-pagination-bottom">
    <h2 id="archive-pagination-bottom" class="visually-hidden">Archive Pages</h2>
    <ul>
      <li>
        {% if page.url != pagination.href.first %}
          <a href="{{ pagination.href.previous }}" title="Previous" class="previous"><span class="picons-regular-angle"></span> <span class="visually-hidden">Previous</span></a>
        {% else %}
          <span class="previous"><span class="picons-regular-angle"></span> <span class="visually-hidden">Previous</span></span>
        {% endif %}
      </li>
    
      {% for pageEntry in pagination.pages %}
        <li>
          <a
            href="{{ pagination.hrefs[ loop.index0 ] }}"
            {% if page.url == pagination.hrefs[ loop.index0 ] %} aria-current="page"{% endif %}
          >
            <span class="visually-hidden">Page </span>{{ loop.index }}
          </a>
        </li>
      {% endfor %}
      
      <li>
        {% if page.url != pagination.href.last %}
          <a href="{{ pagination.href.next }}" title="Next" class="next"><span class="picons-regular-angle"></span> <span class="visually-hidden">Next</span></a>
        {% else %}
          <span class="next"><span class="picons-regular-angle"></span> <span class="visually-hidden">Next</span></span>
        {% endif %}
      </li>
    </ul>
  </nav>
